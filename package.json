{
  "name": "2020",
  "version": "1.0.0",
  "description": "Ⅲ",
  "author": "Shannon Young <shannon@mark-making.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mrshannonyoung/2020"
  },
  "scripts": {
    "serve": "bundle exec jekyll s & gulp dev",
    "serve-quiet": "bundle exec jekyll s --quiet & gulp dev",
    "staging": "bundle exec jekyll clean && JEKYLL_ENV=staging bundle exec jekyll b --config _config.yml,_staging.yml && gulp",
    "build": "bundle exec jekyll clean && bundle exec jekyll b && gulp"
  },
  "dependencies": {
    "browser-sync": "^2.26.12",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.3.0",
    "gulp-dart-sass": "^1.0.2",
    "gulp-load-plugins": "^2.0.5",
    "gulp-sourcemaps": "^2.6.5"
  }
}
